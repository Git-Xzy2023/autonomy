# GET 与 POST 的区别

| 区别 | GET | POST |
|------|-----|------|
| 作用 | 用于获取资源 | 用于传输资源 |
| 参数位置 | 参数拼接在 URL 后面 | 参数放在请求体中 |
| 安全性 | 参数会保留在浏览器历史记录中 | 参数不会被保留 |
| 幂等性 | 是幂等的 | 不是幂等的 |
| 可缓存性 | 可以被浏览器缓存 | 不会被缓存 |
| 数据长度限制 | 有长度限制（取决于浏览器，一般 2-8KB） | 没有限制 |
| 参数数据类型 | 只接受 ASCII 字符 | 没有限制，可为二进制数据 |
