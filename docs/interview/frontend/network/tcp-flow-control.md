# TCP 的流量控制

流量控制是为了让发送方发送数据的速度不要太快，让接收方来得及接收。TCP 采用大小可变的**滑动窗口**进行流量控制，窗口大小单位为字节。

- 连接建立时，每一端分配一个缓冲区保存输入的数据，并将缓冲区大小发送给另一端
- 当数据到达时，接收方发送确认，其中包含自己剩余的缓冲区大小（窗口通告）
- 如果接收方应用程序读数据的速度与数据到达速度一致，接收方会在每一确认中发送一个正的窗口通告
- 如果发送方速度快于接收方，接收方缓冲区会被充满，导致接收方通告一个**零窗口**
- 发送方收到零窗口通告时必须停止发送，直到接收方重新通告一个正的窗口
