---
title: "如何比较两个浮点数「几乎相等」？"
---

# 如何比较两个浮点数「几乎相等」？

用 `Number.EPSILON`：

```js
function nearlyEqual(a, b, eps = Number.EPSILON) {
  return Math.abs(a - b) < eps;
}
0.1 + 0.2 === 0.3;         // false（经典面试题）
nearlyEqual(0.1 + 0.2, 0.3); // true
```

---
