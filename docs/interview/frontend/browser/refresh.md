# F5、Ctrl+F5、地址栏回车有什么区别？

| 操作 | 行为 |
|------|------|
| **F5 刷新** | 对本地缓存文件过期，但带上 If-Modified-Since、If-None-Match 请求头，服务器检查新鲜度，可能返回 304 或 200 |
| **Ctrl+F5（强制刷新）** | 不仅对本地文件过期，且不带条件请求头，相当于从未请求过，返回 200 |
| **地址栏回车** | 按正常流程：本地检查是否过期 → 服务器检查新鲜度 → 返回内容 |
